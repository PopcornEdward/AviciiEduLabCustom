{
  // 🎯 关键修正：使用 "astro/tsconfigs/base" 或 "astro/tsconfigs/strict" 别名
  // 请注意这里的路径是 `tsconfigs` (复数) 而不是 `tsconfig` (单数)!
  "extends": "astro/tsconfigs/strict", 
  "compilerOptions": {
    "target": "ESNext",
    "module": "ESNext",
    
    // **关键修改：将 'node' 升级为 'bundler' 或 'node16'**
    // 这将解决 '找不到 @lucide/astro 或其相应类型声明' 的错误。
    "moduleResolution": "bundler",
    
    // 启用 React/TSX 支持
    "jsx": "react-jsx",
    "jsxImportSource": "react",
    
    "resolveJsonModule": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["src/*"]
    },
    
    "isolatedModules": true,
    "noEmit": true
  }
}